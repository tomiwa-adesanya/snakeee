# Runtime dependencies, and this list is the whole of them. Every version is
# pinned exactly: a dependency in an open source game is a cost paid by every
# contributor, and by the packager on three operating systems.
flask==3.1.3
waitress==3.0.2
pywebview==6.2.1

# The real-time channel between a host and the players who joined it.
websockets==16.1.1
